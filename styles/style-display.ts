import { StyleSheet } from "react-native";

const styleDisplay = StyleSheet.create({
    flex: {
        display: 'flex',
    },
    none: {
        display: 'none',
    },
})

export default styleDisplay;